/*Create a Jquery program to implement a expense tracker to find the total expenses by accepting details on each expenses(expense category and expense ammount). 
- Add the data entered by the user into a table when user click on a button after entering data
- Add an option to delete a record from the tabel to remove the expense category
- calculate and diapaly the total of the expenses every time user */

